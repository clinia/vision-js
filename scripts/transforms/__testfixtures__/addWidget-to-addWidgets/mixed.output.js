/* global vision */

const search = vision();

function someRandomFunction() {}

search.addWidgets([vision.widgets.records({})]);

someRandomFunction();

search.addWidgets([vision.widgets.records({})]);

search.addWidgets([vision.widgets.records({})]);

search.addWidgets([vision.widgets.records({})]);
